# Helper file to handle all configs

[aliases]
test = pytest

[coverage:run]
# .coveragerc to control coverage.py and pytest-cov
omit =
    # Omit the tests
    */tests/*
    # Omit generated versioningit file
    seamm_thermochemistry/_version.py

[flake8]
exclude = docs
max-line-length = 88
extend-ignore = E203
